<div class="form-row">
  <div class="form-item">
    <label>Identifier (Species, Name or Both)</label>
    <input type="text" name="title" value="<%= pet.title %>">
  </div>
  <div class="form-item">
    <label>Owner</label>
    <select name="owner">
      <% owners.forEach(owner => { %>
        <% if (owner.id === pet.owner) { %>
          <option selected label="<%= owner.name %>" value="<%= owner.id %>">
        <% } else { %>
          <option label="<%= owner.name %>" value="<%= owner.id %>">
        <% } %>
      <% }) %>
    </select>
  </div>
</div>

<div class="form-row">
  <div class="form-item">
    <label>Date</label>
    <input type="date" name="publishDate"
          value="<%= pet.publishDate == null ? '' : pet.publishDate.toISOString().split('T')[0] %>">
  </div>
  <div class="form-item">
    <label>Age</label>
    <input type="number" name="pageCount" min="1" value="<%= pet.pageCount %>">
  </div>
</div>

<div class="form-row">
  <div class="form-item form-item-no-grow">
    <label>Picture</label>
    <input type="file" name="cover" class="pet-cover filepond">
  </div>
  <div class="form-item">
    <label>Diagnosis and Treatment</label>
    <textarea name="description"><%= pet.description %></textarea>
  </div>
</div>